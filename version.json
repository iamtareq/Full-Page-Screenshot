{
  "version": "1.1.1",
  "notes": "Fixes the updater getting stuck on \"Retry\" (the installer-generated native-host manifest no longer counts as a dirty working tree); popup footer now shows the running version."
}
